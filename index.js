const mysql = require("mysql");
const inquirer = require("inquirer");

const dbConnect = mysql.createConnection({
    host:
    user:
    password:
    database:
})

switch(){
    case
    {
        break;
    }
    case
    {
        break;
    }
}